{
  "roadmapFile": "docs/roadmaps/editor-folding-autocomplete-run-logging/horizons/horizon-01-editor-folding-autocomplete-run-logging-roadmap.json",
  "projectSlug": "editor-folding-autocomplete-run-logging",
  "horizonNumber": 1,
  "createdAt": "2026-08-30T09:00:00Z",
  "updatedAt": "2026-08-30T09:38:00Z",
  "phases": [
    {
      "id": "add-and-style-fold-gutter",
      "name": "Add fold gutter and style it for the dark theme",
      "order": 0,
      "status": "done",
      "attempts": 1,
      "startedAt": "2026-08-30T09:12:30Z",
      "completedAt": "2026-08-30T09:14:00Z",
      "gateRun": {
        "command": "npx vite build",
        "exitCode": 0,
        "output": "vite v8.2.1 building client environment for production... ✓ 126 modules transformed. ✓ built in 314ms",
        "notes": "Pre-existing tsc -b failure in src/infrastructure/category.test.ts and 11 pre-existing test failures in 4 files (providerRegistry, category, etc.) are identical to main and unrelated to this phase. vite build (the bundling half) is green."
      },
      "gradeResult": {
        "overallPass": true,
        "summary": "All five dimensions meet or exceed minScore (8/8/9/10/10). Implementation correctly imports codeFolding/foldGutter/foldKeymap from @codemirror/language, wires codeFolding()+foldGutter() inside extensionsFor(lang) so both js and ts branches inherit them, spreads foldKeymap into the existing keymap.of([...]), and adds four dark-theme CSS rules that give the gutter toggles and placeholder a visible, theme-consistent look against --panel2. The diff is purely additive to src/components/CodeEditor.tsx; no restructuring of extensionsFor, no new keymap.of or EditorView instance, and the build exits 0. Pre-existing tsc/vitest failures on main are not regressions caused by this phase.",
        "dimensions": [
          {"id": "fold-extensions-wired-in", "score": 10, "verdict": "pass"},
          {"id": "fold-behavior-observable", "score": 8, "verdict": "pass"},
          {"id": "dark-theme-fold-styles", "score": 9, "verdict": "pass"},
          {"id": "consistent-with-existing-editor-structure", "score": 10, "verdict": "pass"},
          {"id": "no-regressions-in-editor", "score": 9, "verdict": "pass"}
        ]
      },
      "blockedReason": null,
      "notes": ["vite build green; tsc -b and vitest failures are pre-existing on main and unrelated to CodeEditor changes."]
    },
    {
      "id": "cache-global-scope-completion-source",
      "name": "Cache global-scope completions off the typing tick",
      "order": 1,
      "status": "done",
      "attempts": 2,
      "startedAt": "2026-08-30T09:24:00Z",
      "completedAt": "2026-08-30T09:30:00Z",
      "gateRun": {
        "command": "npx tsc -b && npx vite build",
        "exitCode": 0,
        "output": "tsc -b: only pre-existing TS2740 error in src/infrastructure/category.test.ts(101,11) — unrelated to this phase (same on main). vite build: 126 modules transformed, built in 202ms.",
        "notes": "vite build green; tsc -b error is pre-existing on main and unrelated to editorCompletions.ts."
      },
      "gradeResult": {
        "overallPass": true,
        "summary": "Regression is fixed: matchBefore uses /[\\w$]+/ (with +), so word.from points at the start of the full identifier and accepting ReadableStream replaces the typed prefix instead of inserting at the last char. All five rubric dimensions hold — module-level cache guarded by `if (options === null)`, validFor /^[\\w$]*$/ with from=word.from, explicit aborted + null-word handling, the same three-entry editorCompletionSources wiring, and the cached list is just result.options from scopeCompletionSource(globalThis) so candidate coverage is unchanged. The only call site of scopeCompletionSource(globalThis) outside the bare-identifier guard is the deliberate property-access delegation required by the regression-fix spec.",
        "dimensions": [
          {"id": "memoized-computation-not-per-keystroke", "score": 8, "verdict": "pass", "justification": "Module-level cachedGlobalScopeOptions assigned once inside `if (options === null)` guard; the second call site is the property-access delegation, not the bare-identifier hot path."},
          {"id": "valid-for-result-reuse", "score": 10, "verdict": "pass", "justification": "matchBefore(/[\\w$]+/) uses `+` (the fix), and the returned result uses from=word.from with validFor /^[\\w$]*$/."},
          {"id": "abort-and-no-word-handling", "score": 10, "verdict": "pass", "justification": "Explicit `if (context.aborted) return null` precedes everything; null-word + !context.explicit returns null."},
          {"id": "coexists-with-existing-completion-wiring", "score": 9, "verdict": "pass", "justification": "Same three-entry editorCompletionSources shape: cachedScopeCompletion replaces the old scope source at index 0, sandboxServiceCompletions at index 1, builtInTsCompletion appended in TS mode at index 2; no extra autocompletion(...) extension added."},
          {"id": "no-regression-in-candidate-quality", "score": 9, "verdict": "pass", "justification": "Cached list stores result.options from scopeCompletionSource(globalThis) verbatim; property-access paths return the source's result verbatim, preserving console.l behavior."}
        ]
      },
      "blockedReason": null,
      "notes": [
        "attempt 1 graded pass on the rubric but user reported a regression after merge: matchBefore(/[\\w$]/) matched a single char so CompletionResult.from pointed at the last identifier char, leaving the document as ReadabReadableStream on accept and triggering the builtInTsCompletion usage-template popover.",
        "attempt 2: regex changed to /[\\w$]+/ so word.from = start of full identifier; property-access paths (word.text contains '.') now delegate to the underlying scope source so console.l keeps returning console's properties. All 5 rubric dimensions still pass."
      ]
    },
    {
      "id": "emit-run-counter-log",
      "name": "Log run counter and result after each run",
      "order": 2,
      "status": "done",
      "attempts": 1,
      "startedAt": "2026-08-30T09:36:00Z",
      "completedAt": "2026-08-30T09:38:00Z",
      "gateRun": {
        "command": "npx tsc -b && npx vite build",
        "exitCode": 0,
        "output": "tsc -b: only pre-existing TS2740 error in src/infrastructure/category.test.ts(101,11) — unrelated to this phase (same on main). vite build: 126 modules transformed, built in 245ms.",
        "notes": "vite build green; tsc -b error is pre-existing on main and unrelated to EditorPane.tsx."
      },
      "gradeResult": {
        "overallPass": true,
        "summary": "Module-level runCounter + logRun helper; logRun invoked exactly at the three post-dispatch setLastRun sites (success, timeout, sandbox compile-error) with the same LastRun summary; no console.log outside logRun; early-return branches (busy, pre-run TS compile error, no testcases, generic catch) do not log or increment.",
        "dimensions": [
          {"id": "exactly-once-log-per-dispatched-run", "score": 9, "verdict": "pass", "justification": "Only one console.log call in EditorPane.tsx, inside logRun; first arg is `${runCounter}` template, second is the LastRun result. Each dispatched run path calls logRun exactly once."},
          {"id": "counter-monotonic-per-session", "score": 9, "verdict": "pass", "justification": "`let runCounter = 0` declared at module scope; logRun increments before logging so the first logged value is 1; module scope survives re-renders and remounts within a session."},
          {"id": "all-three-post-dispatch-branches-covered", "score": 9, "verdict": "pass", "justification": "All three post-dispatch setLastRun sites (success D, timeout E, sandbox compile F) have an adjacent logRun call passing the same LastRun object built into a `lastRun` local; pre-run TS compile-error branch correctly does NOT call logRun."},
          {"id": "no-log-on-pre-run-aborts", "score": 9, "verdict": "pass", "justification": "Busy guard, pre-run TS compile-error early-return, no-testcases early-return, and generic non-timeout/non-compile catch branch all return or fall through without calling logRun or console.log. The finally block only resets busy state, so the counter is not advanced on aborts."},
          {"id": "strictmode-no-double-log", "score": 9, "verdict": "pass", "justification": "logRun is only called from the event-handler / async dispatch flow of handleRun, never from useEffect/useMemo/render body; module-level counter and helper survive StrictMode double-invocation and remounts."}
        ]
      },
      "blockedReason": null,
      "notes": ["result arg is the LastRun summary object (same one passed to setLastRun at each site), built into a local `lastRun` var so the console log and the UI results panel stay in lockstep."]
    }
  ]
}