# Discoveries — editor-folding-autocomplete-run-logging

- 2026-08-30 | horizon 1 | editor config | extensionsFor(lang) in src/components/CodeEditor.tsx is a hand-picked flat extension array (no basicSetup, no Compartment); language switch does a full StateEffect.reconfigure [src/components/CodeEditor.tsx] → new fold/completion extensions just go in extensionsFor() and apply on mount + switch.
- 2026-08-30 | horizon 1 | folding | No fold code anywhere in src; @codemirror/language@6.12.4 exports codeFolding/foldGutter/foldKeymap and lang-javascript supplies foldable ranges for js and ts → item 1 is pure additive, no parser needed, no dep bump.
- 2026-08-30 | horizon 1 | editor theme | editorTheme has custom .cm-gutters styling but no .cm-foldGutter/.cm-foldPlaceholder rules [src/components/CodeEditor.tsx] → fold markers need explicit dark-theme CSS or they render low-contrast.
- 2026-08-30 | horizon 1 | autocomplete | Of the 3 completion sources in editorCompletions.ts, only scopeCompletionSource(globalThis) is synchronous and heavy (walks the global object graph every call, no memoization); sandboxServiceCompletions is cheap and builtInTsCompletion is already async [src/infrastructure/editorCompletions.ts] → the lag fix targets the scope source alone.
- 2026-08-30 | horizon 1 | run dispatch | EditorPane.handleRun has branches A–G; only D (success), E (timeout), F (sandbox compile) are post-dispatch; B/C/G are pre-dispatch aborts [src/components/EditorPane.tsx] → per-run log goes at the 3 setLastRun sites for D/E/F only.
- 2026-08-30 | horizon 1 | run dispatch | handleRun is invoked only by button onClick, never in an effect [src/components/EditorPane.tsx] → a module-level runCounter is StrictMode-safe; a useRef would reset on remount.
- 2026-08-30 | horizon 1 | workers | Only worker in the app is sandbox.worker.ts, imported via `?worker` query with worker:{format:'es'} in vite.config.ts; tsCompiler/tsCheck load TS from CDN on the main thread [src/hooks/useRunCode.ts] → established pattern exists if a future horizon needs a completion worker.
